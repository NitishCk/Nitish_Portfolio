/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export',
  images: {
    unoptimized: true,
  },
  basePath: '/Nitish_Portfolio',
  assetPrefix: '/Nitish_Portfolio',
};

export default nextConfig;
